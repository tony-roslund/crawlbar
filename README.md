# Crawlbar

Crawlbar is a planned free, open-source Mac utility for saving websites into Obsidian as clean Markdown using a user's own Cloudflare account.

This repository starts with the product website. The app itself will use bring-your-own Cloudflare credentials and keep tokens local.
